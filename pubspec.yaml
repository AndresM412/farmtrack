name: farmtrack
description: A new Flutter project.

publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 🔥 Firebase y otras dependencias
  firebase_core: ^3.5.0
  cloud_firestore: ^5.0.0
  firebase_auth: ^5.1.0
  firebase_storage: ^12.1.0  # 📦 Para subir imágenes a Firebase Storage
  image_picker: ^1.1.2       # 📷 Para seleccionar imágenes de la galería o cámara

  # 👇 Otras librerías del proyecto
  provider: ^6.1.2
  material_design_icons_flutter: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
